import { DotDirective } from './dot.directive';

describe('DotDirective', () => {
  it('should create an instance', () => {
    const directive = new DotDirective();
    expect(directive).toBeTruthy();
  });
});
